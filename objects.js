var playlist = {
    'a-ha': 'Take On Me',
    'Panic': 'Death of A Bachelor'
};

function updatePlaylist(playlist, artistName, songTitle){

}


Create a function updatePlaylist that accepts three parameters:
the playlist (an object), an artist name (a string), and a song title. The body of the function should add the
song and artist as a key-value pair to the playlist object. The function should return the whole playlist.
